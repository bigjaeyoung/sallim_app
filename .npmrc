strict-peer-dependencies=false
auto-install-peers=true
# React Native / Expo + Metro bundler need a flat-ish node_modules layout to
# resolve transitive deps like @babel/runtime helpers. The default isolated
# linker symlinks too aggressively for Metro's resolver. Hoisted mimics npm.
node-linker=hoisted
shamefully-hoist=true
